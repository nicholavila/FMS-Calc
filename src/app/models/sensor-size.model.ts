export interface FMSSensorSize {
  active: boolean;
  bearings: number[];
  id: number;
  name: string;
  roller_diameter: string;
  roller_length: string;
  roller_weight: string;
  sensor_type: number;
  url?: string;
}
