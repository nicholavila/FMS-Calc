import { FilterAllPossiblePipe } from './filter-all-possible.pipe';

describe('FilterAllPossiblePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAllPossiblePipe();
    expect(pipe).toBeTruthy();
  });
});
