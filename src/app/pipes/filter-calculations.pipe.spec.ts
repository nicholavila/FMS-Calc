import { FilterCalculationsPipe } from './filter-calculations.pipe';

describe('FilterCalculationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCalculationsPipe();
    expect(pipe).toBeTruthy();
  });
});
