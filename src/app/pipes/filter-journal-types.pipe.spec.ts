import { FilterJournalTypesPipe } from './filter-journal-types.pipe';

describe('FilterJournalTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterJournalTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
