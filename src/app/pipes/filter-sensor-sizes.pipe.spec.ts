import { FilterSensorSizesPipe } from './filter-sensor-sizes.pipe';

describe('FilterSensorSizesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSensorSizesPipe();
    expect(pipe).toBeTruthy();
  });
});
