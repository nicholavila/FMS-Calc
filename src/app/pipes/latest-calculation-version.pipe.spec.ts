import { LatestCalculationVersionPipe } from './latest-calculation-version.pipe';

describe('LatestCalculationVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new LatestCalculationVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
