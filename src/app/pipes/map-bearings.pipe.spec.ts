import { MapBearingsPipe } from './map-bearings.pipe';

describe('MapBearingsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapBearingsPipe();
    expect(pipe).toBeTruthy();
  });
});
