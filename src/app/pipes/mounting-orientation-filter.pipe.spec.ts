import { MountingOrientationFilterPipe } from './mounting-orientation-filter.pipe';

describe('MountingOrientationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MountingOrientationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
