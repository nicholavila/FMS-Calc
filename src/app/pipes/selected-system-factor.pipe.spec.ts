import { SelectedSystemFactorPipe } from './selected-system-factor.pipe';

describe('SelectedSystemFactorPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedSystemFactorPipe();
    expect(pipe).toBeTruthy();
  });
});
