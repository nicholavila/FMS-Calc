import { SensorOrderPipe } from './sensor-order.pipe';

describe('SensorOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
