import { SortProjectAdministrationPipe } from './sort-project-administration.pipe';

describe('SortProjectAdministrationPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProjectAdministrationPipe();
    expect(pipe).toBeTruthy();
  });
});
