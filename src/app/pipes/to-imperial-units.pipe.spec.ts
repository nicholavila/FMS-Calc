import { ToImperialUnitsPipe } from './to-imperial-units.pipe';

describe('ToImperialUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToImperialUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
