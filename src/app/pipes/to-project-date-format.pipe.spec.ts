import { ToProjectDateFormatPipe } from './to-project-date-format.pipe';

describe('ToProjectDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToProjectDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
