import { ToShowNumberPipe } from './to-show-number.pipe';

describe('ToShowNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ToShowNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
